## The is a test repo that was requested by comegetcred  


## The test requirements  

The test is as followed:
Develop a responsive front-end section layout that has 6 items that are structured as 3 rows and 3 columns. The 6 items of data need to be pulled from any API call of your choice.
Each item on click needs to show a modal popup to display more fields from that particular item data entry in detail.  

Due date in 2 days.